// const sub = (a, b) => {
//   return new Promise(resolve => {
//       setTimeout(() => resolve(a - b), 1000);
//   })
// }


export const sub = (a, b) => {
  // setTimeout(() => {
    return a - b;    
  // }, 2000);
};
