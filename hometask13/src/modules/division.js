// const div = (a, b) => {
//   return new Promise(resolve => {
//       setTimeout(() => resolve(a / b), 1000);
//   })
// }

export const div = (a, b) => {
  // setTimeout(() => {
    return a / b;    
  // }, 2000);
};