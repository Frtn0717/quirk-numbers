// const mul = (a, b) => {
//   return new Promise(resolve => {
//       setTimeout(() => resolve(a * b), 1000);
//   })
// }

export const mul = (a, b) => {
  // setTimeout(() => {
  return a * b; 
  // }, 2000);
};